import 'package:flutter/material.dart';

class StatShow extends StatelessWidget {
  const StatShow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
